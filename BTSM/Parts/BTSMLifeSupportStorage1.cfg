PART
{
	// --- general parameters ---

	name = BTSMLifeSupportStorage1
	module = Part
	author = FlowerChild

	// --- asset parameters ---

	// all models offset along z axis to match position of old model
	MODEL
	
	{
	
		model = Squad/Parts/FuelTank/fuelTankSmall/model

		scale = 1.25, 1.25, 1.25
		position = 0.0, 0.0, -1.15
	}

	MODEL
	
	{
		
		model = Squad/Parts/FuelTank/radialRCSTank/model

		scale = 2.0, 2.0, 2.0
		position = 0.0, -0.25, -1.15
	}

	MODEL
	
	{
		
		model = Squad/Parts/FuelTank/radialRCSTank/model

		scale = 2.0, 2.0, 2.0
		position = 0.0, 0.25, -1.15
	}

	rescaleFactor = 1

	// --- node definitions ---

	// definition format is Position X, Position Y, Position Z, Up X, Up Y, Up Z

//	node_attach = 0.0, 0.0, 0.65, 0.0, 0.0, -1.0
	node_attach = 0.0, 0.0, -0.5, 0.0, 0.0, -1.0, 2

	// --- editor parameters ---

	TechRequired = unassigned

	entryCost = 5400
	cost = 800

	category = Pods
	subcategory = 0

	title = KEG-1000 External Life Support Storage Unit

	manufacturer = Kerlington Party Supply Central Inc.

	description = Frustrated at not being able to attach additional life support and/or beverages to your spacecraft and/or pleasure boats?  Angst no more!  These handy and well insulated units will ensure that no matter how long your voyage, your drinks will stay frosty, while hungry sailors and party goers alike will stay well provisioned!  Note: Long stemmed glasses and little umbrellas sold separately.

	// attachment rules: stack, srfAttach, allowStack, allowSrfAttach, allowCollision

	attachRules = 0,1,0,0,0

	// --- standard part parameters ---

	mass = 0.5

	dragModelType = default
	maximum_drag = 0.2
	minimum_drag = 0.2
	angularDrag = 1

	crashTolerance = 10

	maxTemp = 1400

	RESOURCE
	{
		name = LifeSupport
		amount = 1000
		maxAmount = 1000
	}
}